extends ./layout.pug
block content
    h1 Create a new user
    if errors
        each error in errors
          .alert.alert-danger= error


    form(action="/users/create",method="POST")
        .d-flex.aligh-item-center
            label.mr-3 Name
            input(placeholder="Nhập tên user cần tạo" ,name="name",type="text", value=value ? value.name :"")
            button.btn.btn-primary.ml-3 Tạo

    a.btn.btn-outline-danger(href="./") Back

    p this some erros #{errors}